node_modules/
out/
release/
dist/
.DS_Store
*.log
test/.artifacts/

# Local design-review evidence: screenshots of the running app, which show the
# maintainer's real saved sessions. Never commit these.
.impeccable/review/
.impeccable/hook.cache.json

# TypeScript incremental build state
*.tsbuildinfo

# Working notes and design records: useful locally, not part of the published
# project. Kept on disk, never committed.
CLAUDE.md
DESIGN.md
PRODUCT.md
.impeccable/
